{% extends "default.njk" %}

{% block content %}
    <article class="post" data-input-path="{{ page.filePathStem }}">
      <h1 class="post-title">{{ title }}</h1>
{{ content | safe | trim }}
    </article>
{% endblock %}
