{% extends "default.njk" %}

{% block content %}    <article class="post" data-input-path="{{ page.filePathStem }}">
      <h1 class="post-title">{{ title }}</h1>
      <time class="post-date" datetime="{{ date | dateISO }}">{{ date | dateFormat('LL') }}</time>
{{ content | safe }}    </article>{% endblock %}